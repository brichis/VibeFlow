export const getBlockExplorerTxLink = (chainId: number, txHash: string): string => {
  // For now, return empty string since we're not using actual block explorers
  // In a real implementation, you would map chainId to block explorer URLs
  return "";
};
